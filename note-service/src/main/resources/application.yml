server:
  port: 8083

spring:
  application:
    name: note-service
  data:
    mongodb:
      uri: mongodb://${MONGO_USER:root}:${MONGO_PASSWORD:password}@${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DB:abernathy_note}?authSource=admin
